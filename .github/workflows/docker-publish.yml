name: Manual Docker Images

on:
  workflow_dispatch:

permissions:
  actions: read
  contents: read
  packages: write

jobs:
  publish:
    permissions:
      actions: read
      contents: read
      packages: write
    uses: ./.github/workflows/container-publish.yml
    secrets: inherit

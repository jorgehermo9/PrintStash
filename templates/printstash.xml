<?xml version="1.0"?>
<Container version="2">
  <Name>PrintStash</Name>
  <Repository>ghcr.io/xiao-villamor/printstash:latest</Repository>
  <Registry>https://github.com/xiao-villamor/PrintStash/pkgs/container/printstash</Registry>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Support>https://github.com/xiao-villamor/PrintStash/issues</Support>
  <Project>https://www.printstash.org</Project>
  <Overview>PrintStash is a self-hosted 3D print library and G-code vault. This image runs the web UI and API together. Create the first account in the browser from your local network, or set First-run setup to environment and fill in the administrator fields to create it at first start. The one appdata path stores everything PrintStash keeps: the database, uploaded files, thumbnails, staging files, backups, and caches. Add existing model folders as separate Library sources after setup; do not use an existing library as the appdata path.</Overview>
  <Category>Productivity: Tools:</Category>
  <WebUI>http://[IP]:[PORT:3000]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/xiao-villamor/PrintStash/main/templates/printstash.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/xiao-villamor/PrintStash/main/icon.svg</Icon>
  <ReadMe>https://raw.githubusercontent.com/xiao-villamor/PrintStash/main/unraid/README.md</ReadMe>
  <ExtraSearchTerms>3d printer 3d printing model manager stl library 3mf gcode bgcode step stp obj cad file organizer self hosted docker nas klipper moonraker octoprint prusalink bambu elegoo spoolman orcaslicer prusaslicer cura print history</ExtraSearchTerms>
  <ExtraParams>--restart=unless-stopped</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Date>2026-09-25</Date>
  <MinVer>6.10</MinVer>
  <License>AGPL-3.0</License>
  <Requires>Choose how the first administrator is created with First-run setup. With trusted_network (the default), leave the administrator fields blank and open the WebUI from your local network (tower.local or the server's LAN IP) to register in the browser; the first person to register becomes administrator, and registration through Tailscale, a VPN, a reverse proxy or a domain is refused. With environment, fill in the administrator username and password and PrintStash creates that account at first start. If the two do not fit together, setup stays closed and the WebUI explains what to change. When moving from the old two-container install, record the old API data owner first, then stop both old containers. Set PUID and PGID to that same owner (the old image usually used 10001:10001); otherwise existing Library sources can become unreadable. Follow the migration guide.</Requires>
  <Config Name="WebUI Port" Target="3000" Default="3000" Mode="tcp" Description="Port to open PrintStash in your browser." Type="Port" Display="always" Required="true" Mask="false">3000</Config>
  <Config Name="Appdata" Target="/data" Default="/mnt/user/appdata/printstash" Mode="rw" Description="Dedicated persistent folder for everything PrintStash keeps: database, managed files, thumbnails, staging, backups, and caches. Keep it on one pool and add no second mapping for a subfolder of /data; imports then copy each file instead of using a hard-link. Do not map an existing model library here." Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/printstash</Config>
  <Config Name="First-run setup" Target="VAULT_SETUP_MODE" Default="trusted_network|environment" Mode="" Description="How the first administrator is created. trusted_network: in the browser, only from your local network (for example http://tower.local:3000 or the server's LAN IP); leave the administrator fields blank. environment: at first start from the administrator fields below, which are then required; choose this if you will first open PrintStash through Tailscale, a VPN, a reverse proxy or Unraid Connect." Type="Variable" Display="always" Required="true" Mask="false">trusted_network</Config>
  <Config Name="Administrator username" Target="VAULT_SETUP_ADMIN_USERNAME" Default="" Mode="" Description="Only with First-run setup set to environment: creates this administrator at first start; sign in with it and choose where files are stored. Leave blank with trusted_network, which creates the first administrator in the browser from your local network instead. Used once: it never changes an existing account." Type="Variable" Display="always" Required="false" Mask="false"></Config>
  <Config Name="Administrator password" Target="VAULT_SETUP_ADMIN_PASSWORD" Default="" Mode="" Description="Only with First-run setup set to environment: 8 to 256 characters. Used once: editing it later does not change the account; reset it under Settings, Users instead." Type="Variable" Display="always" Required="false" Mask="true"></Config>
  <Config Name="Administrator email" Target="VAULT_SETUP_ADMIN_EMAIL" Default="" Mode="" Description="Optional, only with First-run setup set to environment: email for that administrator." Type="Variable" Display="always" Required="false" Mask="false"></Config>
  <Config Name="User ID" Target="PUID" Default="99" Mode="" Description="Numeric owner for appdata and mounted Library sources. Default is Unraid's nobody user. On upgrade, use the old API data owner's UID to keep existing Library sources readable." Type="Variable" Display="always" Required="false" Mask="false">99</Config>
  <Config Name="Group ID" Target="PGID" Default="100" Mode="" Description="Numeric group for appdata and mounted Library sources. Default is Unraid's users group. On upgrade, use the old API data owner's GID to keep existing Library sources readable." Type="Variable" Display="always" Required="false" Mask="false">100</Config>
  <Config Name="Settings restart" Target="VAULT_RESTART_ENABLED" Default="true" Mode="" Description="Allows Settings to restart the supervised application container." Type="Variable" Display="advanced" Required="false" Mask="false">true</Config>
</Container>
